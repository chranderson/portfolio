export enum MqSize {
  SM = '640px',
  MD = '768px',
  LG = '1024px',
  XL = '1280px',
  XXL = '1536px'
}

export enum SIZE {
  /** mobile */
  SM = `(min-width: 640px)`,
  /** mobile */
  MD = `(min-width: 768px)`,
  /** tablet */
  LG = `(min-width: 1024px)`,
  /** laptop / small desktop */
  XL = `(min-width: 1280px)`,
  /** desktop */
  XXL = `(min-width: 1536px)`
}
